version: '3'
services:
  docker_mysql:
    image: mysql:5.7
    container_name: gastosAppDb
    volumes:
    - "/etc/timezone:/etc/timezone:ro"
    - "/etc/localtime:/etc/localtime:ro"
    environment:
      MYSQL_ROOT_USER: root
      MYSQL_ROOT_PASSWORD: password
      MYSQL_HOST: localhost
    ports:
      - 3306:3306